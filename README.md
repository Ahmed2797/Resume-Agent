# Resume-Agent
Resume-Agent is an AI-based resume evaluation module.  It extracts text from uploaded resumes (PDF/TXT), analyzes them against job descriptions using a language model, and returns a match percentage along with insights.  This module is designed to automate and improve the resume screening process.
